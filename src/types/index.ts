// This file is kept for compatibility, but we've moved all workflow types to src/types/workflow.ts
// You can import directly from there in new code

export type { Workflow, WorkflowNode, NodeConnection, Position } from './workflow'; 